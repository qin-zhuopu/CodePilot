# docker-build.sh 读取的构建参数
# 复制为 .env 并按环境填写(.env 已被 .gitignore 忽略,不会入库)

# npm registry —— 内网 nexus 或公共镜像
NPM_REGISTRY=https://registry.npmmirror.com

# 构建期下载 better-sqlite3 预编译二进制所需的 HTTP 代理
HTTP_PROXY_URL=http://172.24.0.5:3128
